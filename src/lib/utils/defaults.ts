/**
 * Default values and templates for the resume builder
 *
 * Note: This file is being phased out.
 * All resume CSS and HTML templates have been moved to styles.ts.
 *
 * Please import from styles.ts instead:
 * - DEFAULT_RESUME_CSS
 * - DEFAULT_RESUME_MARKDOWN
 */

import { DEFAULT_RESUME_CSS, DEFAULT_RESUME_MARKDOWN } from "./styles";

// Re-export for backward compatibility
export { DEFAULT_RESUME_CSS, DEFAULT_RESUME_MARKDOWN };
