export default async function EditorPage() {
  // const { filename } = await params;
  return (
    <p>Not Finished</p>
    // <MarkdownEditor
    //   initialMarkdown={initialMarkdown || ""}
    //   filename={filename}
    // />
  );
}
